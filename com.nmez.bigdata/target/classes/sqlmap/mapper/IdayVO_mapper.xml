<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">   

<mapper namespace="IdayVO">

	<select id="selectData" parameterType="IdayVO" resultType="hashmap">
		<![CDATA[
		select gu, dong, sector, format(Mon, 0) as Mon, format(Tue, 0) as Tue, 
		format(Wed, 0) as Wed, format(Thu, 0)as Thu, format(Fri, 0) as Fri,
		format(Sat, 0) as Sat, format(Sun, 0) as Sun
		from income_day where gu = #{gu} and dong = #{dong} and sector= #{sector};
		]]>
	</select>
	
	<!-- 	select * from income_day
			where gu = #{gu} and dong = #{dong} and sector = #{sector}; -->

</mapper>